<%= form_for(@panel_plan) do |f| %>
  <% if @panel_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@panel_plan.errors.count, "error") %> prohibited this panel_plan from being saved:</h2>

      <ul>
      <% @panel_plan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
