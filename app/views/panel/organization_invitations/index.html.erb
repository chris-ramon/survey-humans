<h1>Listing panel_organization_invitations</h1>

<table>
  <tr>
    <th>User</th>
    <th>Organization</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @panel_organization_invitations.each do |panel_organization_invitation| %>
  <tr>
    <td><%= panel_organization_invitation.User %></td>
    <td><%= panel_organization_invitation.panel_organization %></td>
    <td><%= link_to 'Show', panel_organization_invitation %></td>
    <td><%= link_to 'Edit', edit_panel_organization_invitation_path(panel_organization_invitation) %></td>
    <td><%= link_to 'Destroy', panel_organization_invitation, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Organization invitation', new_panel_organization_invitation_path %>
