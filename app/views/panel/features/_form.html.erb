<%= form_for(@panel_feature) do |f| %>
  <% if @panel_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@panel_feature.errors.count, "error") %> prohibited this panel_feature from being saved:</h2>

      <ul>
      <% @panel_feature.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
